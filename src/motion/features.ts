// Framer Motion's feature bundle (animation, gestures, layout / shared-element
// transitions), code-split out of the entry chunk and loaded by <LazyMotion>.
export { domMax as default } from 'framer-motion';
